# macOS metadata
.DS_Store
**/.DS_Store

# Godot editor cache - regenerated by `godot --headless --editor --quit`
# CI workflow handles this before export (see .github/workflows/android-build.yml)
.godot/
