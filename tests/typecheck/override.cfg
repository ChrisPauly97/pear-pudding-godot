; Installed as res://override.cfg only for the duration of
; scripts/check-typed-access.sh. Never commit it at the project root: it would
; turn these warnings into errors in every editor and game run.
[debug]

gdscript/warnings/unsafe_method_access=2
gdscript/warnings/unsafe_property_access=2
